"""
Цветовая палитра и типографика NeuroClip.

Стиль: 3D cinematic Neuro Cyberpunk — тёмный фон, неоновые акценты,
чистая геометрия. Без "звёзд" и тёмных плашек под текстом.
"""

# --- Фон ---------------------------------------------------------------
BG_PRIMARY = "#0A0A14"      # основной фон окна (глубокий космический тёмно-синий)
BG_SECONDARY = "#12121F"    # структурные панели (сайдбар, плеер) — едва светлее фона,
                             # НЕ используется как подложка под текст-надписи

# --- Неоновые акценты ----------------------------------------------------
ACCENT_CYAN = "#00E5FF"
ACCENT_MAGENTA = "#FF2FC1"
ACCENT_PURPLE = "#7B2FFF"

# --- Текст ---------------------------------------------------------------
TEXT_PRIMARY = "#E7E9F5"
TEXT_MUTED = "#8B8FA8"

# --- Брендинг создателя (см. ТЗ: неймплейт) -------------------------------
CASPER_RED = "#FF2A2A"
DENIS_YELLOW = "#FFD500"

# --- Статусные цвета -------------------------------------------------------
DANGER = "#FF3B3B"
SUCCESS = "#2FFFA0"

# --- Типографика -----------------------------------------------------------
# Системный шрифт для обычных виджетов (кнопки, слайдеры, текст интерфейса).
# Если "Segoe UI" не установлен в ОС — Tkinter автоматически подставит шрифт
# по умолчанию для платформы. Это штатное поведение Tk, а не ошибка.
FONT_FAMILY_UI = "Segoe UI"

# Шрифт бренд-неймплейта — реальный файл, поставляется вместе с приложением
# (assets/fonts/RussoOne-Regular.ttf, лицензия SIL OFL 1.1), поэтому рендерится
# одинаково на любой ОС независимо от того, какие шрифты установлены у пользователя.
BRAND_FONT_FILENAME = "RussoOne-Regular.ttf"
