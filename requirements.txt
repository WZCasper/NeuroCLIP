customtkinter>=5.2.2
pillow>=10.1.0
opencv-python-headless>=4.9.0.80
requests>=2.31.0
python-dotenv>=1.0.1
mediapipe>=0.10.14

# Опционально: распознавание текста в килфиде (Модуль 2). Без этого пакета
# и системного Tesseract OCR детекция килфида всё равно работает (по факту
# появления новой строки), просто без текста в метаданных.
pytesseract>=0.3.10

# Опционально: понадобится, если подключите обученную модель YOLO под нужные
# классы (см. modules/detectors/yolo_detector.py).
# ultralytics>=8.3.0
