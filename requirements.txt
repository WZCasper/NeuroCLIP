customtkinter>=5.2.2
pillow>=10.1.0
opencv-python-headless>=4.9.0.80
requests>=2.31.0
python-dotenv>=1.0.1
mediapipe>=0.10.14
librosa>=0.10.0
soundfile>=0.12.0
moviepy>=2.0.0

# Опционально: удаление фона на превью (Модуль 4). Без него превью всё равно
# собирается — просто с исходным фоном кадра вместо вырезанного объекта.
# При первом использовании скачивает модель сегментации (~176 МБ).
rembg>=2.0.50

# Опционально: распознавание текста в килфиде (Модуль 2). Без этого пакета
# и системного Tesseract OCR детекция килфида всё равно работает (по факту
# появления новой строки), просто без текста в метаданных.
pytesseract>=0.3.10

# Нужно только для дообучения модели через train_classifier.py — для
# повседневной работы NeuroClip не требуется, поэтому не в основном списке.
# pip install ultralytics>=8.3.0

# Нужно только если включаете чекбокс "Распознавать смех" в интерфейсе —
# тяжёлая зависимость (тянет torch), без неё остальной анализ работает как
# обычно, детектор смеха просто недоступен.
# pip install openai-whisper>=20231117

# Извлечение аудио из видео использует системный ffmpeg (не pip-пакет):
# Linux: sudo apt install ffmpeg | Windows: https://ffmpeg.org/download.html

# Примечание: pip иногда предупреждает о конфликте версий Pillow между
# moviepy и другими пакетами — в проекте это не создаёт реальных проблем,
# можно игнорировать.
